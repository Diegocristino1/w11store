{
  "name": "winning-eleven-store",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-store.mjs",
    "images:manifest": "node scripts/generate-image-manifest.mjs",
    "images:catalog": "node scripts/generate-teams-catalog.mjs",
    "setup:teams": "node scripts/create-team-folders.mjs && node scripts/cleanup-empty-folders.mjs",
    "cleanup:folders": "node scripts/cleanup-empty-folders.mjs && node scripts/generate-image-manifest.mjs && node scripts/generate-teams-catalog.mjs",
    "dev": "node scripts/watch-sync.mjs",
    "build": "node scripts/generate-image-manifest.mjs && node scripts/generate-teams-catalog.mjs && node scripts/sync-store.mjs && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.2",
    "vite": "^6.3.5"
  }
}
